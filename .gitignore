# Environment
.env
venv/

# User content — never overwrite on pull
content/content.json
content/files/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Claude Code local settings
.claude/

# Importer downloaded PDFs (1GB+, not for version control)
importer/files/

# Live-site repo clone — this is the separate GitHub Pages repo, not part of the CMS
live-site/

# Temp file from atomic content.json writes
content/content.tmp

# The built site — ephemeral, rebuilt on each publish
site/

# Python
*.egg-info/
dist/
build/
